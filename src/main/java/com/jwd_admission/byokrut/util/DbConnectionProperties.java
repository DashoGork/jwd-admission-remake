package com.jwd_admission.byokrut.util;

/**
 *Enum contains all keys from application.properties file
 */

public enum DbConnectionProperties {
    defaultPoolSize,
    connectionUrl,
    login,
    password
}
